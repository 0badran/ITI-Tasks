import { CardChangeDirective } from './card-change.directive';

describe('CardChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new CardChangeDirective();
    expect(directive).toBeTruthy();
  });
});
